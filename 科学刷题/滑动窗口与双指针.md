---
date: 2025-04-14
tags:
  - 滑动窗口
  - 双指针
---
>  数组不是单调的话，不要用滑动窗口，考虑用前缀和
# 一、定长滑窗

>==套路：入-更新-出==
>1.入：下表为*i*的元素进入窗口，更新相关统计量。如果*i<k-1*则重复第一步
>2.更新：更新答案。更新最大值最小值。
>3.出;下标为*i-k+1*的元素离开窗口，更新相关统计量
```java
class Solution{
public int maxVowels(string s,int k) {
        int ans = 0 ,vowel = 0 ;
        for( int i = 0 ; i < s.length ; i++) {
            if(“判断新元素是否符合条件”) {
                vowel++;
            }
            if(i < k - 1) {
                continue;
            }
            ans = Math.max(ans,vowel);
            char out = s[i - k + 1];
            if(“判断老元素是否符合条件”) {
                vowel--;
            }
        }
        return ans;
    }
};
```

>就像一只不断蠕动的爬虫

滑动窗口解决线性问题，字符串问题，数字问题，策略问题，动态问题，（这是不是所有算法题的特点）

# 二、不定长滑动窗口
>**注**：滑动窗口相当于在维护一个**队列**。右指针的移动可以视作**入队**，左指针的移动可以视作**出队**。


>套路就是一个for循环抱着一个while
>窗口像一只每次蠕动距离不确定的虫子，碰到不符合条件的数据更新答案，更新答案也有说法，涉及到组合数的知识
## 求最长最大

[1004. 最大连续1的个数 III - 力扣（LeetCode）](https://leetcode.cn/problems/max-consecutive-ones-iii/description/)
```java
class Solution {
    public int longestOnes(int[] nums, int k) {
        int ans = 0;
        int cnt0 = 0;
        int left = 0;
        for (int right = 0; right < nums.length; right++) {
            cnt0 += 1 - nums[right]; // 0 变成 1，用来统计 cnt0
            while (cnt0 > k) {
                cnt0 -= 1 - nums[left];
                left++;
            }
            ans = Math.max(ans, right - left + 1);
        }
        return ans;
    }
}
```

## 求最短最小
[2875. 无限数组的最短子数组 - 力扣（LeetCode）](https://leetcode.cn/problems/minimum-size-subarray-in-infinite-array/description/)
```java
class Solution {
    public int minSizeSubarray(int[] nums, int target) {
        long total = 0;
        for (int x : nums) {
            total += x;
        }

        int n = nums.length;
        int ans = Integer.MAX_VALUE;
        long sum = 0;
        int left = 0;
        for (int right = 0; right < n * 2; right++) {
            sum += nums[right % n];
            while (sum > target % total) {
                sum -= nums[left % n];
                left++;
            }
            if (sum == target % total) {
                ans = Math.min(ans, right - left + 1);
            }
        }

        return ans == Integer.MAX_VALUE ? -1 : ans + (int) (target / total) * n;
    }
}
```
## 求子数组个数最大/最小
求字数组的个数常常设计统计字数组的个数，但因为在滑动窗口模板里，经常用==ans += left - right + 1==或者==ans += left ==获得答案
### 越短越合法
>==ans += left - right + 1==
[713. 乘积小于 K 的子数组 - 力扣（LeetCode）](https://leetcode.cn/problems/subarray-product-less-than-k/description/)
```java
class Solution {
    public int numSubarrayProductLessThanK(int[] nums, int k) {
        int n = nums.length, ret = 0;
        int prod = 1, i = 0;
        for (int j = 0; j < n; j++) {
            prod *= nums[j];
            while (i <= j && prod >= k) {
                prod /= nums[i];
                i++;
            }
            ret += j - i + 1;
        }
        return ret;
    }
}
```
### 越长越合法
>==ans += left

[2537. 统计好子数组的数目 - 力扣（LeetCode）](https://leetcode.cn/problems/count-the-number-of-good-subarrays/description/)
```java
class Solution {
    public long countGood(int[] nums, int k) {
        long ans = 0;
        Map<Integer, Integer> cnt = new HashMap<>();
        int pairs = 0;
        int left = 0;
        for (int x : nums) {
            int c = cnt.getOrDefault(x, 0);
            pairs += c; // 进
            cnt.put(x, c + 1);
            while (pairs >= k) {
                x = nums[left];
                c = cnt.get(x);
                pairs -= c - 1; // 出
                cnt.put(x, c - 1);
                left++;
            }
            ans += left;
        }
        return ans;
    }
}
```
### 恰好型滑动窗口
计算有多少个元素和恰好等于k，相当于把问题变成：
- 计算有多少个元素和>=k的数组
- 计算有多少个元素和>=k + 1的数组

# 三、单序列双指针
## 相向双指针
两个指针分别从前从后向中间移动，滑动窗口相当于同向双指针
[42. 接雨水 - 力扣（LeetCode）](https://leetcode.cn/problems/trapping-rain-water/description/)
```java
class Solution {
    public int trap(int[] height) {
        int ans = 0;
        int preMax = 0; // 前缀最大值，随着左指针 left 的移动而更新
        int sufMax = 0; // 后缀最大值，随着右指针 right 的移动而更新
        int left = 0;
        int right = height.length - 1;

        while (left < right) {
            preMax = Math.max(preMax, height[left]);
            sufMax = Math.max(sufMax, height[right]);
            if (preMax < sufMax) {
                ans += preMax - height[left];
                left++;
            } else {
                ans += sufMax - height[right];
                right--;
            }
        }
        return ans;
    }
}
```
## 同向双指针
[2234. 花园的最大总美丽值 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-total-beauty-of-the-gardens/description/)
## 背向双指针
[1793. 好子数组的最大分数 - 力扣（LeetCode）](https://leetcode.cn/problems/maximum-score-of-a-good-subarray/description/)
## 原地修改
[27. 移除元素 - 力扣（LeetCode）](https://leetcode.cn/problems/remove-element/description/)
```java
class Solution {
    public int removeElement(int[] nums, int val) {
        int k = 0 ;
        for(int i = 0 ; i < nums.length ; i++){
            if(nums[i] != val){
                nums[k] = nums[i] ;
                k++;
            }
        }
        return k;
    }
}
```
>有一个快指针，有一个慢指针

# 四、双序列双指针
## 双指针
[2540. 最小公共值 - 力扣（LeetCode）](https://leetcode.cn/problems/minimum-common-value/description/)
```java
class Solution {
    public int getCommon(int[] nums1, int[] nums2) {
        int i = 0; // 指针 for nums1
        int j = 0; // 指针 for nums2
        
        while (i < nums1.length && j < nums2.length) {
            if (nums1[i] < nums2[j]) {
                i++;
            } else if (nums1[i] > nums2[j]) {
                j++;
            } else {
                return nums1[i]; // 找到最小公共值
            }
        }
        return -1; // 没有公共元素
    }
}
```
>整了两个指针

## 判断子序列
[1023. 驼峰式匹配 - 力扣（LeetCode）](https://leetcode.cn/problems/camelcase-matching/description/)
```java
class Solution {
    public List<Boolean> camelMatch(String[] queries, String pattern) {
        int n = queries.length;
        List<Boolean> res = new ArrayList<Boolean>();
        for (int i = 0; i < n; i++) {
            boolean flag = true;
            int p = 0;
            for (int j = 0; j < queries[i].length(); j++) {
                char c = queries[i].charAt(j);
                if (p < pattern.length() && pattern.charAt(p) == c) {
                    p++;
                } else if (Character.isUpperCase(c)) {
                    flag = false;
                    break;
                }
            }
            if (p < pattern.length()) {
                flag = false;
            }
            res.add(flag);
        }
        return res;
    }
}
```
# 五、三指针
[2563. 统计公平数对的数目 - 力扣（LeetCode）](https://leetcode.cn/problems/count-the-number-of-fair-pairs/description/)
```java
class Solution {
    public long countFairPairs(int[] nums, int lower, int upper) {
        Arrays.sort(nums);
        return count(nums, upper) - count(nums, lower - 1) ;
        
    }
    public long count(int[] nums , int upper) {
        int p = 0 , q = nums.length - 1 ;
        long ans = 0 ;
        while( p < q ){
            if( nums[p] + nums[q] <= upper ){
                ans += q - p;//这是个组合数，i和[i+1 ,j]组合
                p++;
            }else {
                q--;
            }
        }
        return ans ;
    }
}
```
>这里借鉴了一点点恰好滑动窗口转换的思想

# 六、分组循环
适用场景：按照题目要求，数组会被分割成若干组，每一组的判断/处理逻辑是相同的。
核心思想：
- 外层循环负责遍历组之前的准备工作（记录开始位置），和遍历组之后的统计工作（更新答案最大值）。
- 内层循环负责遍历组，找出这一组最远在哪结束。
这个写法的好处是，各个逻辑块分工明确，也不需要特判最后一组（易错点）。以我的经验，这个写法是所有写法中最不容易出 bug 的，推荐大家记住。
[978. 最长湍流子数组 - 力扣（LeetCode）](https://leetcode.cn/problems/longest-turbulent-subarray/description/)
```java
class Solution {
    public int maxTurbulenceSize(int[] arr) {
        int n = arr.length;
        if (n == 1) return 1;
        int ans = 1;
        int i = 0;

        while (i < n - 1) { // 修改循环条件，i最多到n-2
            if (arr[i] == arr[i + 1]) {
                i++; // 相等则跳过，无法作为湍流起点
                continue;
            }

            int start = i; // 记录当前窗口起始位置
            i++; // 至少两个元素，i先移动到i+1

            // 扩展窗口：确保有i+1和i+2（即至少三个元素参与判断），并且满足湍流条件
            while (i < n - 1) {
                long diffPrev = (long) arr[i] - arr[i - 1]; // 前一对元素的差
                long diffNext = (long) arr[i] - arr[i + 1]; // 当前对准各检查的一对元素的差
                // 湍流条件：相邻差异的乘积为负，说明符号交替
                if (diffPrev * diffNext > 0) {
                    i++;
                } else {
                    break;
                }
            }
            // 计算窗口长度。从start到i（包含i）都是满足条件的
            ans = Math.max(ans, i - start + 1);
            // 如果是因为不满足条件而break，i可能会停留在当前窗口的最后一个满足条件的元素上
            // 下一轮循环从i开始，因为i和i+1可能是一个新窗口的起点
            // 如果是因为到达数组末尾而结束，i也会自然退出
        }
        return ans;
    }
}
```

# 七、总结
你的观察非常精准！滑动窗口和双指针确实有相似之处（都涉及端点操作），但核心差异在于**问题解决范式和移动逻辑**。下面通过对比帮你彻底厘清：

---

### 🧩 核心差异总结

|​**特性**​|​**滑动窗口**​|​**双指针（广义）​**​|
|---|---|---|
|​**本质**​|双指针的子集（同向指针）|更广泛的算法思想（含同向/相向/背向）|
|​**移动方向**​|左右指针**同向移动**​（右扩左缩）|可同向/相向/背向移动|
|​**问题类型**​|连续子区间问题（子数组/子串）|搜索、区间处理、全局优化问题|
|​**决策依据**​|窗口**内部状态**​（如字符计数）|可能依赖**全局状态**​（如前后缀最值）|
|​**典型例题**​|最长无重复子串、定长子数组最大和|接雨水、三数之和、有序数组两数和|

---

### 🔍 深入解析你的疑问

#### 1. ​**为何接雨水更适合相向双指针？​**​

- ​**滑动窗口局限**​：窗口内只关注局部状态（如窗口和），但接雨水需要**同时依赖左右两侧的全局最大值**​（`preMax`和`sufMax`）。
    
- ​**双指针优势**​：
    
    ```
    while (left < right) {
        preMax = Math.max(preMax, height[left]);  // 全局左侧最大值
        sufMax = Math.max(sufMax, height[right]); // 全局右侧最大值
        if (preMax < sufMax) { 
            ans += preMax - height[left]; // 决策依赖全局状态
            left++;
        } else { ... }
    }
    ```
    
- 💡 关键：双指针的移动由**外部全局变量驱动**，而滑动窗口由**窗口内状态驱动**。
    

#### 2. ​**滑动窗口真的是“局部”吗？​**​

- 滑动窗口通过遍历所有可能窗口来获得全局解，但**决策仅依赖窗口内状态**。
    
    _例如：定长滑窗求最大元音数_
    
    ```
    for (int i=0; i<s.length; i++) {
        if (isVowel(s[i])) vowel++;     // 只依赖新入元素
        if (i>=k-1) {
            ans = max(ans, vowel);      // 全局答案更新
            if (isVowel(s[i-k+1])) vowel--; // 只依赖移出元素
        }
    }
    ```
    
- ✅ ​**结论**​：滑动窗口的**决策是局部的**，但**结果收集是全局的**。
    

---

### 🚀 如何选择算法？

|​**场景**​|​**推荐算法**​|​**原因**​|
|---|---|---|
|连续子数组/子串（e.g. 最长无重复子串）|滑动窗口|天然适合区间连续性问题|
|依赖外部状态（e.g. 接雨水）|相向双指针|需同时考虑左右全局信息|
|搜索匹配（e.g. 有序数组两数和）|相向双指针|两端向中间收敛高效|
|数组原地修改（e.g. 移除元素）|同向双指针|快慢指针覆盖写操作|

---

### 💎 总结你的核心洞察

> ​**​“双指针的全局性 vs 滑动窗口的局部性”​**​
> 
> - 双指针（尤其相向）能更**全面比较端点与全局状态**​（如接雨水中的`preMax/sufMax`），适合非连续依赖问题。
>     
> - 滑动窗口的**强项是维护窗口内状态**，通过同向指针蠕动高效解决子区间问题，但决策不依赖外部变量。
>     

​**建议**​：

1. 遇到**子数组/子串问题**优先想滑动窗口
    
2. 遇到**依赖左右全局状态或搜索问题**优先考虑相向双指针
    
3. 遇到**数组原地操作**考虑同向快慢指针